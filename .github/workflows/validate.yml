name: validate

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  validate:
    name: Validate palette + shader + ports
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Run validation script
        run: bash scripts/validate.sh
